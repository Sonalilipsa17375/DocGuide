{
  "name": "documentation-generator",
  "version": "0.1.0",
  "description": "Automatically generates documentation for your project",
  "main": "out/extension.js",
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.18.63",
    "jsdoc-api": "^8.0.0",
    "typescript": "^4.0.3",
    "vscode": "^1.1.37"
  },
  "engines": {
    "vscode": "^1.50.0"
  },
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "docguide.generateDocs",
        "title": "Generate Documentation"
      }
    ]
  }
}
